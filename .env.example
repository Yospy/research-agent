# Copy to .env and fill in your keys. The app reads these at startup and fails fast if missing.
# The values are NEVER read or logged anywhere except when calling the APIs.

OPENAI_API_KEY=
EXA_API_KEY=

# optional — defaults to gpt-5.1
# OPENAI_MODEL=
