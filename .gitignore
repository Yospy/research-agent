# secrets — never commit
*.env
!.env.example

# deps / build
node_modules
dist

# go orchestrator build artifact (binary named after the dir)
orchestrator/orchestrator

# local runtime data (durable store + traces)
research.db*
logs/

# os
.DS_Store